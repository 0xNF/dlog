extension StringBuilder on StringBuffer {
  int indexOf(String string, [int startPosition = 0]) {
    // TODO(nf): implement
    return -1;
  }

  int indexOfAny(Iterable<String> chars, [int startPosition = 0]) {
    // TODO(nf): implement
    return -1;
  }
}
